package com.lhh.image;

import java.awt.image.BufferedImage;
import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;

import javax.imageio.ImageIO;

import org.junit.Test;

public class TestLhhUtilsImage {

	@Test public void testReplaceICADWithWhiteColor(){
		/*String parent = "C:\\Users\\huage\\Downloads\\";
		String path = parent+"sfz_zm.png";
		File file = new File(path);
		FileInputStream input = null;
		try {
			input = new FileInputStream(file);
			BufferedImage bi = ImageIO.read(input);
			bi = LhhUtilsImage.replaceICADWithWhiteColor(bi);
			ImageIO.write(bi, "png", new File(parent+"sfz_zm2.png"));
		} catch (IOException e) {
			e.printStackTrace();
		} finally{
			if( input != null ){
				try {
					input.close();
				} catch (IOException e) {
					e.printStackTrace();
				}
			}
		}*/
	}
	
}
